const Now = () => {
  return (
    <section id="now" className="mx-auto max-w-2xl px-4 py-20 sm:px-8">
      <p className="font-mono text-sm text-slate">now — august 2026</p>
      <p className="mt-4 text-lg leading-relaxed text-bone">
        Building doodlydoo, a canvas two people can draw on at once —
        Yjs, WebSocket, Postgres, private invite links. Shipping
        iykykprints orders the rest of the week.
      </p>
    </section>
  );
};

export default Now;
